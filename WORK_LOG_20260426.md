# 2026-04-26 작업 일지 (WORK LOG)

## 오늘 한 작업 (Completed)
1. **PDF 수출 기능 완전 개편 및 렌더링 안정화**
   - 기존의 불안정했던 `html2canvas` 렌더링을 완전히 제거하고, `jsPDF`를 이용해 프로그래밍 방식으로 깔끔한 A4 리포트를 직접 그리도록 재설계했습니다.
   - 불필요한 디자인 요소를 빼고 **텍스트와 표 중심**으로 구성하여 렌더링 오류를 차단했습니다.
   - 타로 카드 이미지도 전체를 넣는 대신 **선택한 카드 3장**만 깔끔하게 나열되도록 최적화했습니다.
   - 폰트 최적화: `NotoSansKR` 폰트를 subset(경량화)하여 TTF 형태로 PDF에 직접 임베딩하여 **한글 깨짐 현상을 완벽히 해결**했습니다.
   - 관련된 모든 `any` 타입 경고와 ESLint 에러를 수정하여 높은 코드 품질을 확보했습니다.

2. **12지신 민화 일러스트 작업 완료**
   - 어제 쿼터 제한으로 완료하지 못했던 나머지 6종(말, 양, 원숭이, 닭, 개, 돼지)의 일러스트 생성을 완료했습니다.
   - 타로 프레임이 없는 깔끔한 **영물(anthropomorphic) 컨셉**의 고퀄리티 웹툰 작화를 유지했습니다.
   - 12종의 이미지를 전부 저용량 JPEG(`400x400`)로 포맷 변환 후 UI에서 일괄적으로 `.jpg`를 로딩하도록 최적화했습니다.

3. **작업 사항 커밋 및 배포**
   - 위 변경 사항을 `dev/tarot-rebranding` 브랜치에 커밋 후, `main`으로 Merge하고 원격 저장소에 Push 했습니다.
   - Firebase App Hosting 파이프라인을 통해 프로덕션 자동 배포가 완료되었습니다.

## 다음에 할 작업 (Next Steps)
1. **메인 화면의 도사 캐릭터 동적화 (다양성 부여)**
   - **목표**: 첫 화면에 고정적으로 뜨는 1개의 정적인 도사 이미지를 대신해, 매번 로딩 시 랜덤하게 변하는 **10종의 젊고 트렌디한 도사 이미지**를 배치합니다.
   - **컨셉**: K팝 데몬 헌터스(K-Pop Demon Hunters) 캐릭터 느낌의 "잘생긴 젊은 남자 + 현대적이고 세련된 도사 + 전통 한복의 어레인지" 작화.
   - **현재 상황**: 오늘 첫 번째 이미지(`dosa_v2_01.png`) 생성 후 **이미지 생성 AI 쿼터 초과(429 Error)**로 작업이 일시 중단되었습니다. (약 4시간 반 뒤 쿼터 리셋 예정)
   - **다음 세션 진행 순서**:
     1. 남은 9개의 젊은 도사 일러스트(`dosa_v2_02` ~ `dosa_v2_10`) 생성 완료하기.
     2. 생성된 이미지들을 JPEG로 최적화하여 `/public/assets/dosas/`에 배치.
     3. `src/app/page.tsx`를 수정하여 화면 진입 시 1~10번 중 랜덤한 일러스트가 매번 새롭게 표시되도록 UI 로직 업데이트하기.
